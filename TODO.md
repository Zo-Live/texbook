# 项目改进 TODO

## 标题与日期

- [x] 设计标题来源策略：默认文件名、手动 `--title`、LLM 自动生成标题。
- [x] 实现单文件 `extract` 手动指定标题。
- [x] 实现单文件 `extract` 使用 LLM 生成标题。
- [x] 实现批量 `batch` 使用 LLM 为每个文件生成标题。
- [x] 设计并实现标题生成 prompt，要求输出稳定、简短、适合作为 LaTeX 标题。
- [x] 实现 tex 文件日期显示控制，默认隐藏日期，并支持显式显示日期。

## Prompt 预设

- [x] 抽象当前中文数学讲义 prompt 为内置预设。
- [x] 设计预设数据结构，覆盖系统 prompt、用户 chunk prompt、标题生成 prompt、额外说明等需要扩展的部分。
- [x] 在 `extract` 和 `batch` 中添加选择预设的 CLI 参数。
- [x] 实现终端交互式新增预设功能。
- [x] 为交互式输入添加友好说明，避免直接暴露内部变量名。
- [x] 设计预设保存位置、命名规则、覆盖规则和校验规则。
- [x] 更新缓存键，确保不同预设、不同标题策略不会误用旧缓存。

## 文件类型与异常处理

- [ ] 梳理 PyMuPDF 打开、读取、渲染不同文件时可能抛出的异常。
- [ ] 增加项目内文档提取异常类型，包装 PyMuPDF 输入文件错误。
- [ ] 优化单文件转换失败提示，避免直接暴露 Python traceback。
- [ ] 优化批量转换错误处理：跳过无法读取的文件，继续处理其他文件。
- [ ] 批量转换结束时显式汇总失败数量、失败文件和原因，并返回非零退出码。
- [ ] 检查 LLM 请求、缓存读写、输出写入等其他潜在异常边界。

## 文档与测试

- [ ] 更新 README 中标题、日期、预设和批量错误处理的使用说明。
- [ ] 增加标题来源相关单元测试。
- [ ] 增加日期显示控制测试。
- [x] 增加 prompt 预设加载、选择和交互式新增测试。
- [ ] 增加 PyMuPDF 异常包装和批量跳过失败文件测试。
- [ ] 运行项目验证命令，至少包括 `uv run pytest`。
